# Краткая сводка: Поворот игрока к курсору

## Дата: 26 ноября 2025

## ✅ Выполнено

Реализован поворот текстуры игрока в направлении курсора мыши для сервера и клиента.

## Изменения

### Сервер (Zero_Ground/Zero_Ground.cpp)
**Строка:** ~5067

**Добавлено:**
- Получение позиции мыши в мировых координатах
- Вычисление угла через `atan2(dy, dx)`
- Применение поворота через `setRotation(angle + 90°)`

### Клиент (Zero_Ground_client/Zero_Ground_client.cpp)
**Строка:** ~3637

**Добавлено:**
- Аналогичная логика для клиента
- Поворот работает независимо от камеры

## Алгоритм

```cpp
// 1. Получить позицию мыши
sf::Vector2i mousePixelPos = sf::Mouse::getPosition(window);
sf::Vector2f mouseWorldPos = window.mapPixelToCoords(mousePixelPos);

// 2. Вычислить вектор направления
float dx = mouseWorldPos.x - playerPos.x;
float dy = mouseWorldPos.y - playerPos.y;

// 3. Вычислить угол (в градусах)
float angle = std::atan2(dy, dx) * 180.0f / 3.14159f;

// 4. Применить поворот (-90° для компенсации начальной ориентации)
sprite.setRotation(angle - 90.0f);
```

## Почему -90°?

- Текстура изначально смотрит **вверх** (↑)
- В SFML: 0° = вправо (→), -90° = вверх (↑)
- Поэтому вычитаем 90° из вычисленного угла

## Особенности

✅ Мгновенный поворот (без анимации)  
✅ Работает во всех направлениях (360°)  
✅ Учитывает положение камеры  
✅ Работает при движении игрока  
✅ Работает после респавна  

## Компиляция

**Сервер:** ✅ 0 ошибок  
**Клиент:** ✅ 0 ошибок  

## Тестирование

1. Запустите сервер и клиент
2. Двигайте мышь вокруг игрока
3. Проверьте что спрайт всегда смотрит на курсор
4. Проверьте поворот при движении (WASD)
5. Проверьте поворот после респавна

## Результат

🎯 Текстура игрока теперь всегда смотрит на курсор мыши, что делает игру более интуитивной и улучшает игровой опыт!
