# Проникновение пуль через деревянные стены

## Что изменилось

Теперь пули ведут себя по-разному в зависимости от типа стены:

### 🧱 Бетонные стены (серые)
- Пули **останавливаются** при столкновении
- Цвет: серый
- Вероятность появления: 70%

### 🪵 Деревянные стены (коричневые)
- Пули **проходят насквозь**
- Цвет: коричневый
- Вероятность появления: 30%

## Как это работает

1. Когда пуля летит по карте, система проверяет столкновения со стенами
2. Если пуля попадает в стену, определяется её тип
3. **Бетонная стена** → пуля уничтожается
4. **Деревянная стена** → пуля продолжает полёт

## Технические детали

### Новый метод проверки столкновений
```cpp
WallType checkCellWallCollision(const std::vector<std::vector<Cell>>& grid)
```

Этот метод:
- Определяет, в какой ячейке находится пуля
- Проверяет стены в текущей и соседних ячейках
- Возвращает тип стены при столкновении

### Логика обработки
```cpp
for (auto& bullet : activeBullets) {
    WallType hitWallType = bullet.checkCellWallCollision(grid);
    
    // Останавливаем пулю только при попадании в бетон
    if (hitWallType == WallType::Concrete) {
        bullet.range = 0.0f;
    }
    // Деревянные стены игнорируются - пуля летит дальше
}
```

## Изменённые файлы

1. **Клиент:** `Zero_Ground_client/Zero_Ground_client.cpp`
2. **Сервер:** `Zero_Ground/Zero_Ground.cpp`

В обоих файлах:
- Добавлен новый метод `checkCellWallCollision()`
- Обновлена логика обработки столкновений пуль

## Как протестировать

1. Запустите сервер и клиент
2. Найдите на карте:
   - Серые стены (бетон)
   - Коричневые стены (дерево)
3. Стреляйте в них и наблюдайте разницу:
   - Пули останавливаются на сером
   - Пули проходят через коричневое

## Замедление пуль (v1.1)

### 🎯 Новая механика
При прохождении через деревянную стену:
- **Скорость пули** уменьшается на 50%
- **Дальность** также уменьшается на 50%
- **Множественные стены:** эффект накапливается

### Примеры
```
AWP через 1 стену: 2000 px/s → 1000 px/s
AWP через 2 стены: 2000 px/s → 500 px/s
AWP через 3 стены: 2000 px/s → 250 px/s
```

## Стратегические возможности

Эта механика открывает новые тактические возможности:
- **Укрытие за бетоном** - полная защита от пуль
- **Укрытие за деревом** - частичная защита (пули замедляются)
- **Стрельба через дерево** - можно поражать врагов, но пуля медленнее
- **Множественные стены** - 2-3 деревянные стены почти как бетон
- **Выбор позиции** - важно учитывать тип и количество стен вокруг

## Производительность

Оптимизация:
- Проверяется только 9 ячеек (текущая + 8 соседних)
- Нет проверки всей карты
- Быстрый выход при обнаружении столкновения

## Совместимость

✅ Полностью совместимо с существующей системой
✅ Не требует изменений сетевого протокола
✅ Работает на клиенте и сервере одинаково
