# Changelog - Всплывающие подсказки для патронов

## [1.0.0] - 27.11.2025

### Добавлено
- ✅ Функция `renderAmmoTooltip()` для отображения подсказок патронов
- ✅ Обработка наведения мыши на слоты патронов в магазине
- ✅ Отображение названия, цены и количества патронов
- ✅ Список совместимых типов оружия для каждого типа патронов
- ✅ Автоматическое позиционирование подсказки на экране
- ✅ Единый визуальный стиль с подсказками оружия

### Изменено
- 🔄 Функция `renderShopUI()` - добавлена поддержка подсказок патронов
- 🔄 Добавлена переменная `hoveredAmmo` для хранения наведённого слота
- 🔄 Логика отрисовки подсказок - теперь поддерживает и оружие, и патроны

### Технические детали

#### Новые функции
```cpp
void renderAmmoTooltip(sf::RenderWindow& window, 
                       const AmmoItem* ammo, 
                       float mouseX, 
                       float mouseY, 
                       const sf::Font& font)
```

#### Изменённые переменные
```cpp
// Было:
Weapon* hoveredWeapon = nullptr;
float hoveredMouseX = 0.0f;
float hoveredMouseY = 0.0f;

// Стало:
Weapon* hoveredWeapon = nullptr;
AmmoItem* hoveredAmmo = nullptr;  // Новая переменная
float hoveredMouseX = 0.0f;
float hoveredMouseY = 0.0f;
```

#### Добавленная логика
- Проверка наведения мыши на каждый слот патронов
- Создание временного объекта `AmmoItem*` при наведении
- Отрисовка подсказки поверх всех UI элементов
- Автоматическое удаление объекта после отрисовки

### Информация о совместимости

#### Bullets 9x18 → Пистолеты
- USP
- Glock-18
- Five-SeveN
- R8 Revolver

#### Bullets 5,45x39 → Винтовки
- Galil AR
- M4
- AK-47

#### Bullets 7,62x54 → Снайперские винтовки
- M10
- AWP
- M40

### Визуальные параметры

**Размеры подсказки:**
- Ширина: 320px
- Высота: 220px
- Отступы: 15px

**Цветовая схема:**
- Фон: RGB(20, 20, 20, 240)
- Рамка: RGB(255, 215, 0) - золотая, 2px
- Название: RGB(255, 215, 0) - золотой, жирный
- Цена: RGB(100, 255, 100) - светло-зелёный
- Количество: RGB(255, 255, 255) - белый
- Список оружия: RGB(150, 200, 255) - голубой

**Размеры шрифтов:**
- Название: 24px
- Цена: 20px
- Количество: 18px
- Заголовок списка: 18px
- Список оружия: 16px

### Позиционирование

**Правила:**
1. По умолчанию: +20px справа и снизу от курсора
2. Если выходит за правый край: слева от курсора
3. Если выходит за нижний край: сверху от курсора
4. Резерв снизу для UI: 150px
5. Минимальные отступы от краёв: 10px

### Производительность

**Оптимизации:**
- Объекты `AmmoItem*` создаются только при наведении
- Автоматическое удаление после отрисовки
- Нет утечек памяти
- Минимальное влияние на FPS

### Тестирование

**Проверено:**
- ✅ Отображение подсказок для всех типов патронов
- ✅ Корректность информации о совместимости
- ✅ Адаптивное позиционирование на экране
- ✅ Переключение между слотами
- ✅ Взаимодействие с подсказками оружия
- ✅ Визуальное оформление
- ✅ Производительность

### Файлы

**Изменённые:**
- `Zero_Ground_client/Zero_Ground_client.cpp` - реализация для клиента
- `Zero_Ground/Zero_Ground.cpp` - реализация для сервера

**Созданные:**
- `AMMO_TOOLTIP_FEATURE.md` - описание функциональности
- `AMMO_TOOLTIP_TESTING.md` - руководство по тестированию
- `ПОДСКАЗКИ_ПАТРОНОВ.md` - краткая сводка на русском
- `CHANGELOG_AMMO_TOOLTIP.md` - этот файл

### Связанные задачи

- Связано с: Система покупки патронов
- Связано с: Подсказки для оружия
- Связано с: UI магазина оружия

### Известные ограничения

1. Подсказка не отображается при закрытом магазине
2. Одновременно может быть активна только одна подсказка
3. Подсказка исчезает при закрытии магазина

### Будущие улучшения

Возможные доработки:
- [ ] Анимация появления/исчезновения подсказки
- [ ] Отображение текущего запаса патронов игрока
- [ ] Подсветка совместимого оружия в инвентаре
- [ ] Звуковой эффект при наведении

---

**Автор:** Kiro AI Assistant  
**Дата:** 27 ноября 2025  
**Версия:** 1.0.0
